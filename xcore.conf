# xcore/xcore.conf
# xcorekit barrel config — all public tools listed here.
# xcore/install.sh uses this to clone + install everything.
# ─────────────────────────────────────────────────────────────────────────────

# Public xcorekit repos (cloned as siblings of xcore/)
declare -a XCOREKIT_PUBLIC=(
    "git-core"
    "bash-core"
    "animate-core"
    "calendar-core"
    "finance-core"
    "backup-core"
    "sys-core"
)
